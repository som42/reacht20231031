import React from "react";

function App(props) {
  // HOOK : use 로 시작하는 함수
  // hook 은 컴포넌트 상단에 작성해야함
  // 조건문(if), 반복문(for), 중첩된 함수안 에 작성하면 안됨

  if (true){

  }

  function handle() {
    // hook 호출 코드 불가
  }
  return <div></div>;
}

export default App;
